query seeFeed($lastId: Int) {
  seeFeed(lastId: $lastId) {
    ...PhotoFragment
    owner {
      username
      avatar
    }
    caption
    comments {
      ...CommentFragment
    }
    createdAt
    isMine
    isLiked
  }
}
